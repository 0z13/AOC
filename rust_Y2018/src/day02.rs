#![feature(slice_group_by)]
pub fn sol() {
    let lines: Vec<&str> = include_str!("./data/day02.txt").lines().collect();

    let mut twos = 0;
    let mut threes = 0;
    for i in lines {
         
    }
}

